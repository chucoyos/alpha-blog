<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Alpha Bloggers", root_url, :class => "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, :class => "nav-link" %>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if logged_in? %>
              <li>
                <%= link_to "Create new article", new_article_path, :class => "dropdown-item" %>
              </li>
              <li><hr class="dropdown-divider"></li>
            <% end %>
            <li>
              <%= link_to "View articles", articles_path, :class => "dropdown-item" %>
            </li>

          </ul>
        </li>

        <li class="nav-item dropdown">
          <% if logged_in? %>
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= "(Admin)" if current_user.admin? %> <%= current_user.username %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            
                <li>
                  <%= link_to "View Profile", user_path(current_user), :class => "dropdown-item" %>
                </li>

                <li><hr class="dropdown-divider"></li>

                <li>
                  <%= link_to "Edit Profile", edit_user_path(current_user), :class => "dropdown-item" %>
                </li>

                <li><hr class="dropdown-divider"></li>

                <li class="nav-item">
                  <%= link_to "Delete my account", user_path(current_user), method: :delete, data: { confirm: "Your account and all asociated articles will be destroyed, Are you sure?" }, :class => "dropdown-item" %>
                </li>

              </ul>

              <li class="nav-item">
                <%= link_to "Logout", logout_path, method: :delete, data: { confirm: "Do want to leave?" }, :class => "nav-link" %>
              </li>

          <% else %>
                
                <li><hr class="dropdown-divider"></li>

                <li>
                  <%= link_to "Login", login_path, :class => "nav-link" %>
                </li>

                <li>
                  <%= link_to "Signup", signup_path, :class => "nav-link" %>
                </li>
              
          <% end %>
            
            
        </li>

        

      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>